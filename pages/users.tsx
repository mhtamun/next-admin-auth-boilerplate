import React from 'react';
import { DataTable } from '../components';

const users = () => {
    return (
        <>
            <DataTable />
        </>
    );
};

export default users;
